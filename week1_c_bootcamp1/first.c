#include <stdio.h>  //includes the standard IO library


int main() {        //defines the main function (entry point)
    int a = 10;    // defines an integer varaible a and intialises it to 10
    printf("%d", a);   //format-prints the value of a as an integer (%d is a format specifier)
    return 0;  //returns from function with error code zero

}


