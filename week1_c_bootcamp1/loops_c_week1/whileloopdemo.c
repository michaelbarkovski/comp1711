#include <stdio.h>

int main() {
    int a = 0;

    while (a < 5)

    {
        printf("a is equal to %d\n", a);
        a++;
    }

    printf("I've finished the loop and a is equal to %d\n", a);

    return (0);
}

// a is initalised to zero
// ● Enter the WHILE loop and test if a < 5
// ● Print out value of a
// ● Increment a
// -
// ● Repeat the loop